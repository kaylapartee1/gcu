# gcu
# Beauty Supply Vending Machine Mobile App - Starter Code
Files included:
- package.json (React Native client dependencies)
- src/ (React Native app screens and components)
- functions/ (Firebase Cloud Functions for server-side APIs)
- firestore_schema.json (Firestore collections and docs layout)
- architecture.md (System architecture diagram in Mermaid format)
- api_endpoints.md (API endpoints documentation)

**Notes**
- Replace placeholder Firebase config in src/firebaseConfig.js with your project's values.
- Install dependencies and run the React Native app per your usual workflow.
- Deploy functions with Firebase CLI: `firebase deploy --only functions`.
